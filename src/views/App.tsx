import React from "react";
import {Helmet} from "react-helmet";

const App = () => {
    return (
        <div style={{ height: "100%" }}>
            <Helmet>
                <title>Webinar Builder</title>
            </Helmet>
        </div>
    );
};

export default App;
